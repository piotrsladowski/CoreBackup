<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="CoreBackup.Views.EventLogView">

  <DockPanel>
    <TextBlock Text="--- Notifications and Events ---" TextAlignment="Center" FontSize="16" Margin="1" Grid.Row="0" DockPanel.Dock="Top"/>
    <ItemsControl Items="{Binding LogsData}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border BorderThickness="0" Margin="2">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
              </Grid.RowDefinitions>

              <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="100"/>
              </Grid.ColumnDefinitions>


              <TextBox Text="{Binding Content}"
                            Grid.Row="1" IsEnabled="False"
                            Margin="30,0,0,0"/>
          
            </Grid>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
  </DockPanel>
</UserControl>
